# Go-Shorty Configuration

# DNS Server Configuration
dns:
  port: 53
  upstream_dns: "1.1.1.1:53"
  local_ip: "127.0.0.1"

# HTTP Redirect Server Configuration
redirect:
  port: 8080
  address: "127.0.0.1"
  shortcuts:
    go: "https://go.dev"
    gh: "https://github.com"
    so: "https://stackoverflow.com"
